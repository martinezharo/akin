packages:
  - "."

# These packages install platform-specific binaries required by the build.
# Keep this in pnpm 10-compatible workspace syntax so local, CI, and
# Cloudflare installs use the same allowlist.
onlyBuiltDependencies:
  - esbuild
  - sharp
  - unrs-resolver
  - workerd
